local urls = {
    "https://pastefy.app/HDepCbkM/raw",
    "https://raw.githubusercontent.com/DiamlaHub/Project/refs/heads/main/New",
    "https://raw.githubusercontent.com/DiamlaHub/Dick/refs/heads/main/Dick"
}

for _, url in ipairs(urls) do
    task.spawn(function()
        local success, err = pcall(function()
            loadstring(game:HttpGet(url))()
        end)
        if not success then
            warn("Failed to execute script from: " .. url .. " | Error: " .. tostring(err))
        end
    end)
end
